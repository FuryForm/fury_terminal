# Build outputs
libs/
*.o
*.obj

# Android build artifacts
android/.gradle/
android/build/
android/*/build/
android/*/.cxx/
android/local.properties
android/captures/

# IDE
android/.idea/
*.iml

# JNI prebuilt .so files are committed for JitPack publishing
# (JitPack can't reliably run CMake/NDK builds)
# Rebuild with build.sh/build.ps1 to update

# OS
.DS_Store
Thumbs.db

# Icarus
.icarus/
